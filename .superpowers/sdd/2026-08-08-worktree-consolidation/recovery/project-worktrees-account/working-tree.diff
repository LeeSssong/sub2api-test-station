diff --git a/docs/project/project-progress.md b/docs/project/project-progress.md
index 650d9c364..dc9a68d4f 100644
--- a/docs/project/project-progress.md
+++ b/docs/project/project-progress.md
@@ -32,7 +32,7 @@
 9. **Monitor V2 卡片信息精简**：已合并移除各指标样本数量、模型折叠区及接口 `models` 字段，保留性能指标、有效调用、P95 与趋势；前后端测试已通过，但尚未推送、部署或生产验证。**状态：工程差异待部署**。
 10. **Monitor 当前状态与飞书历史错误码修复**：已按“最新一次渠道探测”口径完成本地实现与回归，修正最新成功被历史失败压过，以及 `balance_exhausted` 残留导致账号正在运行却被告警为无可用账号。**状态：工程差异待部署（本地验证通过，待推送、部署和线上验证）**。
 11. **`api.xingqiaolab.top` TLS 兼容性修复**：Caddy RSA-2048/TLS 1.2/1.3 策略已部署，但 CC Switch 刷新仍失败。2026-08-01 完成 nginx TLS 前置层受控生产切换，Compose、Caddy、nginx 及未受影响服务门禁通过；但受影响 Mac 上 TLS 1.2 仍在 ClientHello 阶段被重置，CC Switch 3.18.0 实际刷新仍显示“查询失败”，因而已回滚到 Caddy 直接接管 80/443。同一公网 IP 上仅 SNI `api.xingqiaolab.top` 的 TLS 1.2 被重置，替换 SNI、无 SNI 及服务器本机访问均成功，根因已收敛为客户端到源站之间的域名特异网络干预。**状态：进行中（nginx 代码已推送、生产试部署已回滚；待使用兼容域名或新公网/CDN 入口完成客户端验收）**。
-12. **账号监控分组经营与评分规则**：正在为 Sub2API 原生账号监控增加每组持久化评分权重、分组倍率/公开状态/原生顺序投影及受保护的权重管理路由；本地实现与测试尚未推送、部署或线上验证。**状态：进行中（未部署）**。
+12. **账号监控分组经营与评分规则**：正在为 Sub2API 原生账号监控增加每组持久化评分权重、分组倍率/公开状态/原生顺序投影及受保护的权重管理路由；Task 6 正在重建前端经营数据合同、深色账号卡片、评分编辑弹窗和 30 天账务历史抽屉。本地实现与测试尚未推送、部署或线上验证。**状态：进行中（未部署）**。
 
 ## 生产工程代码/配置已部署并验证（18 项）
 
diff --git a/docs/superpowers/specs/2026-08-01-account-monitor-group-operations-design.md b/docs/superpowers/specs/2026-08-01-account-monitor-group-operations-design.md
index cfce7fcb0..874029d79 100644
--- a/docs/superpowers/specs/2026-08-01-account-monitor-group-operations-design.md
+++ b/docs/superpowers/specs/2026-08-01-account-monitor-group-operations-design.md
@@ -1,136 +1,111 @@
-# 账号监控：分组经营与质量排序设计
+# 账号监控：全站经营、分组运营与账号质量设计
 
 **日期：** 2026-08-01（Asia/Shanghai）  
-**状态：** 已确认设计，待用户审阅书面规格后再进入实施计划
+**状态：** 产品设计已确认；待用户审阅本书面规格后再编写实施计划。
 
 ## 目标
 
-把管理员账号监控页改造成以分组为入口的运营监控台。管理员应能在一个页面中：
+将管理员账号监控页重构为一个用于运营决策的页面：先判断全站是否健康、是否赚钱；再按分组观察服务质量与投入产出；最后使用账号质量分辅助查看每个分组内的账号。质量分和分组排序均只影响监控展示，绝不改变生产调度。
 
-1. 先看到全站或某分组的服务质量与经营结果；
-2. 再下钻到账号，判断单账号的稳定性、成本、营收与利润；
-3. 使用质量评分决定**监控展示顺序**，但绝不隐式改变生产调度或流量分配。
-
-## 已确认的产品规则
-
-- 页面使用 `全站` 与各分组的 Tab；不再将所有分组的账号卡片平铺在同一页。
-- `全站` 永远在第一个 Tab；其余分组按分组质量分由高到低排列。
-- 分组排名只影响账号监控页的 Tab 与分组展示顺序，不更新 `groups.sort_order`，不影响用户可见顺序、账号调度或流量分配。
-- 账号卡片的唯一可编辑调度字段仍是 Sub2API 原生 `accounts.priority`。数值越小，原生调度优先级越高。
-- 不创建可编辑的“分组优先级”或第二套账号调度优先级。
-- 本组中的账号名次为只读信息；它以原生全局账号优先级为依据，不能编辑。
-- 质量分只用于监控排序、健康判断、告警与运营决策；不参与实际调度。
-- 默认重点为“今日”，今日的统计窗口为服务器配置时区的当天 `00:00` 至当前时间；同时必须默认可见“历史累计”，不能把今日 0 误解为历史数据删除。
-
-## 页面层级
+## 一、页面层级与排序
 
 ```text
 账号监控
-├── 全站 Tab
-│   ├── 今日 + 历史累计：成本、营收、利润、利润率、请求、Token
-│   └── 全站去重账号列表
-└── 分组 Tab（按分组质量分排序）
-    ├── 分组状态、质量分、展示名次、可用性与账务汇总
-    ├── 当前分组的评分规则
-    ├── 正常账号卡片（按账号质量分排序）
-    ├── 不可用账号折叠区
-    └── 已停用/未启用账号说明区
+├── 全站经营总览（默认“今日”，同时显示历史累计）
+├── 固定全站摘要栏（进入分组后保持可见）
+├── 分组 Tab（按分组倍率从高到低）
+│   └── 当前分组的服务健康 + 经营结果 + 评分规则
+└── 当前分组的账号卡片
+    ├── 正常、成本合格账号（按账号质量分排序）
+    ├── 不可用 / 成本不合格 / 待确认账号折叠区
+    └── 暂停账号说明区
 ```
 
-## 顶部总览与 Tab
-
-页面标题保持“账号监控”，副标题明确为“按分组比较服务质量与投入产出；真实请求优先，探测数据补位”。右侧保留“立即刷新探测”，并新增低强调的“账务历史”入口。该入口始终以当前 Tab 为统计范围：选择“全站”时查看全站按日账务，选择分组时查看该分组按日账务。
-
-顶部总览保留深色大统计卡的视觉语言，展示：
-
-- 全部账号：参与监控数与已暂停数；
-- 当前可用：数量与可用率；
-- 当前不可用：数量与按原因归类的摘要；
-- 待确认：没有近期真实请求且探测过期的账号数；
-- 今日经营：上游成本、用户营收、利润、利润率；
-- 历史累计：上游成本、用户营收、利润、利润率。
-
-“账务历史”不在主页面再铺一组图表，点击后从右侧打开轻量抽屉。抽屉默认显示最近 30 天，支持切换 7 天、30 天和自定义时间范围；每天一行显示请求、Token、上游成本、用户营收、利润与利润率，并在顶部展示所选区间合计。更早数据通过范围查询或分页继续查看。账号卡片底部既有“历史”动作复用该抽屉，但范围收窄到当前账号。
-
-分组 Tab 采用截图中横向胶囊筛选项的风格，显示“分组名 · 可用 N / 不可用 N · 质量分”。`全站` Tab 显示去重后的全部账号池；某账号属于多个分组时，在全站只出现一次。
+- `全站`为首个入口；分组 Tab 统一按分组倍率从高到低，倍率相同则保持 Sub2API 原有稳定顺序。
+- 不创建分组质量分，不按分组质量分排序，不创建可编辑的分组优先级。
+- 分组排序只影响账号监控页展示，不写入 `groups.sort_order`，不影响用户可见顺序、账号调度或流量分配。
+- 账号的唯一可编辑调度字段仍是 Sub2API 原生 `accounts.priority`。它是全局调度优先级，不是监控评分权重。
 
-## 分组经营头部
+## 二、全站与分组经营视图
 
-每个分组 Tab 在账号列表上方展示一条分组经营头部：
+### 全站首屏
 
-- 分组名称、状态、暴露状态、质量分、展示名次；
-- 可用 / 不可用 / 待确认账号数量；
-- 今日：请求、Token、上游成本、用户营收、利润、利润率；
-- 历史累计：上游成本、用户营收、利润、利润率；
-- “评分规则”入口，用于调整本分组的监控评分权重。
+默认时间范围为服务器配置时区当日 `00:00` 至当前时间，并在同一屏低强调显示历史累计；点击“账务历史”后从右侧打开抽屉，查看按日数据（默认最近 30 天，支持 7 天、30 天和自定义范围）。
 
-“暴露状态”并非单一数据库字段，而是依据分组状态、专属分组授权、订阅类型与有效订阅综合判断。`inactive` 分组仅显示“已关闭”，不发异常告警；活跃但没有向任何有效用户开放的分组只显示一次运营提示；活跃、已开放且无可用账号的分组才进入异常状态。真实用户请求失败可覆盖该抑制条件。
+全站总览固定分为两个区块：
 
-## 账号卡片：截图为视觉基准
+| 区块 | 指标 |
+|---|---|
+| 经营结果 | 上游真实扣费、用户实际计费、纸面利润、利润率、成本覆盖率、待对账笔数 |
+| 服务健康 | 可用、不可用、待确认、暂停账号数量；成功率、TTFT P50、总耗时 P95 |
 
-账号卡片必须以现有账号监控的深色卡片组件和截图为基线复用、扩展，不新建一套普通统计卡或表格来替代。实现时应保留该图的双列卡片布局、层级、边框、色彩、状态区和信息密度；新增字段只嵌入既有信息区：
+进入任一分组后，全站总览收缩为固定摘要栏，继续显示今日成本、营收、利润、成本覆盖率和待对账，避免用户失去全局感。
 
-- 左侧状态色边：绿色表示可用，红色表示不可用，灰色/虚线表示暂停或未启用；
-- 卡片头部：账号名、ID、平台标签、可用状态；右上角为醒目的综合评分；
-- 评分下方：本组质量名次、证据置信度、真实请求或探测样本说明；
-- 四格核心指标：账号倍率、成功率、TTFT P50、总耗时 P95；
-- “最近 5 分钟监测情况”保留为条形时间序列，并显示成功数或探测补位次数；
-- 今日用量行：请求、Token；
-- 今日投入产出行：账号成本、用户扣费、利润、利润率；
-- 累计投入产出以第二行、较低视觉层级紧随今日数据展示：累计成本、累计营收、累计利润、累计利润率；
-- 底部保留数据来源、样本数、更新时间与刷新/历史等动作。
+### 分组视图
 
-卡片中唯一可编辑项为“账号权重”，其文案应明确为“全局调度优先级”，以避免将其误解为评分权重。编辑成功后，页面立即重新拉取并计算该账号在当前分组中的只读优先级名次；质量排序不随该字段改变而被覆盖。
+每个分组 Tab 的头部复用同样的两类信息，但统计范围只限当前分组：
 
-## 评分与排序
+- 服务健康：可用 / 不可用 / 待确认 / 暂停数量、成功率、TTFT P50、总耗时 P95；
+- 经营结果：请求、Token、上游真实扣费、用户实际计费、纸面利润、利润率、成本覆盖率、待对账；
+- 低强调展示历史累计，并提供当前分组范围的“账务历史”入口；
+- “评分规则”入口用于编辑本分组的账号质量评分权重。
 
-### 账号质量分
+分组没有质量评分。其异常语义如下：
 
-每个分组拥有独立的四项评分权重：
+- `inactive` 或当前未向任何有效用户开放的分组显示“已关闭”或一次性运营提示，不作为故障反复报警；
+- 已开放且无可用账号的分组才进入异常状态；真实用户请求失败可覆盖“未开放”的抑制条件。
 
-- 账号倍率（成本）；
-- 成功率；
-- TTFT；
-- 总耗时。
+## 三、账号卡片
 
-四项权重必须为非负整数并合计 100%。新分组默认使用成本 30%、成功率 30%、TTFT 20%、总耗时 20%。评分规则仅影响该分组账号的质量分、该分组质量分及页面展示排序。
+账号卡片必须复用现有渠道监控的深色卡片、双列布局、状态边色和“最近 5 分钟监测情况”绿色柱状时间序列；不能替换为普通统计卡或表格。
 
-账号需要满足“当前可调度、探测结果新鲜、评分所需证据充分”才进入正常排序。不可用账号不参与评分，进入独立的红色折叠区；已暂停或未启用账号显示在独立说明区，不计入可用/不可用/评分。
+每张卡片分为两个可扫描区域：
 
-### 分组质量分
+| 区域 | 内容 |
+|---|---|
+| 服务质量 | 质量评分、组内质量名次、成功率、TTFT P50、总耗时 P95、数据来源、置信度、样本数、更新时间 |
+| 投入产出 | 上游真实扣费、用户实际计费、纸面利润、利润率、成本覆盖率、待对账信息；今日为主、历史累计为次级行 |
 
-分组质量分由该组可参与评分账号的质量分汇总得出。默认按该账号在当前分组的真实请求占比加权；无真实请求时，退回为等权平均。为了避免无证据分组占据前列：
+- 卡片中部保留绿色监测柱状图：明确不可用时才使用红色；可用但较弱仍用绿色深浅和柱高表达；无数据或过期使用中性灰。
+- 真实账单未覆盖完整时，利润须标记“待对账”，不能用倍率估算伪造真实利润。
+- 卡片中“账号权重”可直接编辑 Sub2API 原生 `priority`；保存后立即刷新原生优先级显示，但不得改变质量分排序。
 
-- 没有足够新鲜探测或真实请求证据的分组标记“证据不足”，不进入正常名次；
-- 不可用账号不会被隐藏，而是通过可用数量和异常状态显式暴露；
-- 同分时按可用账号数、随后按分组名称稳定排序。
+## 四、账号质量评分
 
-排名是自动计算、只读的监控排名。管理员无需手动拖拽或保存排名；更新评分规则或刷新探测后，相关 Tab、分组与账号立即按新的质量分重新排序。
+质量分只存在于账号层面。不可用、暂停、成本不合格或证据过期账号不进入正常排序，分别显示在独立折叠区或说明区。
 
-## 账务口径与去重
+每个分组拥有独立、持久化的四项权重；它们只决定该分组中账号的质量分和展示排序：
 
-全站和分组均直接聚合请求流水，而非累加账号卡片或分组卡片：
+| 指标 | 新分组默认权重 | 含义 |
+|---|---:|---|
+| 成本优势 | 15 | 账号倍率相对当前分组倍率的安全余量 |
+| 成功率 | 45 | 当前统计窗口内成功请求比例 |
+| TTFT P50 | 20 | 首 Token 延迟中位数 |
+| 总耗时 P95 | 20 | 总耗时的 P95 |
 
-- 全站：每条 `usage_logs` 仅计入一次，按真实 `account_id`、成本与用户扣费字段聚合；账号跨组不会重复。
-- 分组：按每条请求真实记录的 `usage_logs.group_id` 聚合；账号可跨组归属，但一次请求只记入实际服务的那个分组。
-- 未带 `group_id` 的流水只计入全站，并在全站经营区单独标为“未归属请求”，以解释分组之和与全站可能存在的差额。
-- 利润 = 用户营收 - 上游成本；利润率 = 利润 / 用户营收。用户营收为 0 时，利润率显示为 `—`，不显示误导性的 0%。
+- 权重为非负整数，必须合计 100。
+- 评分规则只提供四项直接百分比配置、合计校验、保存与“恢复默认”；不提供特惠、均衡、Pro 等预设模板。
+- 成本优势不是实际利润率。其在当前分组的得分依据为：`15 × (分组倍率 - 账号倍率) ÷ 分组倍率`，结果限制在 0–15 分。
+- 实际利润率仅由真实账单计算：`(用户实际计费 - 上游真实扣费) ÷ 用户实际计费`，在投入产出区展示并受成本覆盖率约束。
+- 同一账号位于多个分组时，因分组倍率与分组评分权重不同，可有不同的成本优势、质量分与展示名次；其真实调度优先级保持全局唯一。
+- 评分证据优先采用当前分组最近 5 分钟真实请求；样本不足时可使用账号全局服务样本补充，但必须标记“全局样本补充”。置信度独立显示，不计入 100 分。
 
-## 数据与交互边界
+## 五、账务与跨分组口径
 
-- 卡片与分组均清楚标识“今日（服务器时区）”和“历史累计”。
-- 每项经营数值应返回其时间窗与更新时间；刷新探测只更新服务质量证据，不得修改账务流水。
-- 账务历史抽屉只读取按日聚合后的账务数据；默认不加载重型趋势图，避免干扰账号监控的主工作流。
-- 全站与分组汇总需要独立的批量聚合接口，避免前端逐卡片相加或产生 N+1 请求。
-- 评分规则需要独立、受管理员权限保护的持久化配置；不得借用或覆盖原生账号优先级、分组显示排序或调度模型路由配置。
-- 所有新接口继续遵守管理员权限与现有敏感字段脱敏规则，不向浏览器输出账号凭据、Cookie、上游 URL 或原始响应。
+- 上游真实扣费来自 New API / Sub2API 的逐笔真实账单；用户实际计费来自本站请求 `actual_cost`。
+- 纸面利润 = 用户实际计费 - 上游真实扣费；利润率 = 纸面利润 / 用户实际计费。用户实际计费为 0 时显示 `—`。
+- 成本按上游 `attempt` 计入，失败重试若已产生上游费用也必须计入；不能只算最终成功账号。
+- 全站按请求流水去重，每条请求只计一次；账号跨分组不造成全站重复。
+- 分组按请求真实 `usage_logs.group_id` 聚合；每条请求只进入实际服务的一个分组。
+- 缺少 `group_id` 的请求只计入全站，并在全站经营区说明为“未归属请求”，以解释分组总和和全站的差额。
+- 成本覆盖率是已匹配真实成本的 attempt / 应对账 attempt。实时允许短暂“待对账”；请求结束超过 10 分钟仍未对齐则标记“对账异常”。
 
-## 验收标准
+## 六、实现边界与验收
 
-1. 管理员可在全站与任一分组间切换；分组 Tab 和分组列表按质量分自动排序。
-2. 修改评分规则后，仅监控质量分与展示顺序变化；账号实际原生调度优先级和分组流量行为不变。
-3. 同一账号跨多个分组时，全站请求、成本、营收、利润不重复；分组数据按实际请求 `group_id` 归属。
-4. 账号卡复用已确认的深色监控卡视觉与信息层级，并显示服务质量、今日投入产出和累计投入产出。
-5. 卡片可直接编辑全局调度优先级；本组名次只读且编辑后刷新。
-6. 次日零点后，今日数据归零但历史累计仍可见，页面明确说明统计口径。
-7. 停用、未暴露和真实异常分组分别呈现已确认的不同告警语义。
+1. 评分规则必须有独立的管理员受限存储与接口；不得复用 OpenAI 原生调度器 `scheduler_score_weights`、账号 `priority` 或分组排序字段。
+2. 全站、分组和账号的经营汇总必须由后端聚合接口提供；前端不得累加卡片数据，也不得复用旧 `today_stats` 作为真实上游成本。
+3. 刷新探测只更新服务质量证据；账务刷新必须先采集上游真实账单、对账后再更新经营汇总。
+4. 修改一个分组的评分规则后，只重算该分组内账号的质量分与展示排序；不影响其他分组和生产调度。
+5. 同一账号跨分组时，全站请求、成本、营收、利润不重复；分组经营数据按真实 `group_id` 归属。
+6. 今日归零后，历史累计仍可见；历史抽屉按当前全站、分组或账号范围读取按日汇总数据。
+7. 账号卡片保留既有渠道监控视觉样式，并同时清晰展示服务质量与投入产出。
diff --git a/upstream/sub2api/frontend/package.json b/upstream/sub2api/frontend/package.json
index 1d4aaf3ae..52a02eb03 100644
--- a/upstream/sub2api/frontend/package.json
+++ b/upstream/sub2api/frontend/package.json
@@ -58,12 +58,5 @@
     "vite-plugin-checker": "^0.9.1",
     "vitest": "^2.1.9",
     "vue-tsc": "^2.2.0"
-  },
-  "pnpm": {
-    "overrides": {
-      "js-cookie": "3.0.7",
-      "form-data@<4.0.6": ">=4.0.6",
-      "postcss@<8.5.18": ">=8.5.18"
-    }
   }
 }
