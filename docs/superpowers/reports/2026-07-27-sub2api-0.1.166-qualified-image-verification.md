# Sub2API 0.1.166 Qualified Image Verification

**Date:** 2026-07-27
**Status:** qualified image delivered; administrator update intentionally pending

## Scope

- Import official `v0.1.166` from commit
  `dc893dd0b8eab41df5be595ae9fcd1aa74a062b8`.
- Preserve the current Xingqiao Sub2API customizations.
- Build and qualify `xingqiao-sub2api:upstream-0.1.166` for `linux/amd64`.
- Reduce production BuildKit cache with bounded buildx retention.
- Load the qualified image on production, then stop before the administrator
  manually submits the update.

The run does not call `/api/v1/admin/system/update`, edit the production
Compose files, recreate containers, or mutate application data.

## Source Qualification

- Previous upstream source: official `v0.1.165`,
  `e9a58c1cb8b5ef626a75c93b4d953fde5e67aa29`.
- Imported upstream source: official `v0.1.166`,
  `dc893dd0b8eab41df5be595ae9fcd1aa74a062b8`.
- The three-way merge completed with zero textual conflicts.
- The official tagged source still contains `0.1.165` in
  `backend/cmd/server/VERSION`; the qualified image injects runtime version
  `0.1.166` with Docker build arguments.

## Validation

Passed before the merge:

- `go -C sub2api-updater test ./... -count=1`
- `bash tests/operations/update_sub2api_host_test.sh`

Passed after the merge:

- focused backend package tests for config, handlers, repositories,
  securityaudit, server, and services;
- full backend `go test ./... -count=1`;
- full backend `go vet ./...`;
- frontend frozen dependency installation with pnpm `9.15.9`;
- full frontend test suite: `204` files and `1387` tests;
- frontend production build with the Dockerfile layout and
  `NODE_OPTIONS=--max-old-space-size=3072`;
- `bash upstream/sub2api/deploy/test-caddyfile-cache.sh`;
- `bash tests/operations/update_sub2api_host_test.sh`;
- `go -C sub2api-updater test ./... -count=1`;
- `git diff --check`.

The production build requires more than the Dockerfile's current 1536 MiB
frontend Node heap default. A 4 GiB isolated Colima build profile and
`FRONTEND_NODE_MAX_OLD_SPACE_SIZE=3072` are therefore used for this release.
This is a build-time setting only and does not increase the final container's
runtime memory limit.

## Qualified Image

- Tag: `xingqiao-sub2api:upstream-0.1.166`
- Source commit:
  `8e7919aa038fcbbe7752072968c59dee7fe32b7f`
- Local image index:
  `sha256:1217739613058ab9aa2047de7f848b1e45eef681003aec0586274ba8ba1c8145`
- `linux/amd64` platform manifest:
  `sha256:e146027d59ab96c40d9ef12eea3943446a22b32e7aba918b85313909efce4ccf`
- Content size: approximately 41.6 MB

The image labels were inspected before delivery and again on production:

- `com.xingqiao.sub2api.qualified=true`
- `com.xingqiao.sub2api.upstream.version=0.1.166`
- `com.xingqiao.sub2api.upstream.commit=dc893dd0b8eab41df5be595ae9fcd1aa74a062b8`
- `com.xingqiao.sub2api.source.commit=8e7919aa038fcbbe7752072968c59dee7fe32b7f`

Both local and production isolated runs of `/app/sub2api --version` reported
`Sub2API 0.1.166`, upstream commit
`dc893dd0b8eab41df5be595ae9fcd1aa74a062b8`, and build time
`2026-07-27T13:55:22Z`.

## Production Build Cache

Before cleanup:

- root filesystem: 59 GiB total, 40 GiB used, 17 GiB available, 71%;
- Build Cache: 29.36 GB across 636 records, 16.29 GB reclaimable;
- Images: 5.219 GB;
- running Sub2API: healthy qualified `0.1.165`, restart count 0.

Executed:

```text
docker buildx prune --force --max-used-space 5gb --reserved-space 3gb
```

BuildKit reported 11.18 GB released. After cleanup and qualified-image load:

- root filesystem: 59 GiB total, 30 GiB used, 28 GiB available, 53%;
- Build Cache: 18.18 GB across 416 records;
- BuildKit split: 13.07 GB shared with image layers and 5.116 GB private;
- Images: 5.38 GB after intentionally adding the qualified image;
- containers: 6 active, unchanged;
- volumes: 8 total and 6 active, unchanged.

The remaining 13.07 GB shared portion is retained by images. No image prune,
volume prune, container prune, `docker system prune`, or `buildx prune --all`
was used.

## Production Delivery

Docker's direct save of the buildx image index stalled before emitting the
complete stream. Delivery therefore used a single-platform `linux/amd64`
export. The 40,856,310-byte gzip artifact had SHA-256
`b170528ee002dacaea70b7a4ce01c9cf07fe42ed86a111f65a571bd7c8569f70`.
Because the production SSH data channel had very low sustained throughput,
the artifact was split into 16 fixed parts, transferred with independent
connections, and reassembled as a stream. The production-side combined
SHA-256 exactly matched the local artifact before `docker load`.

Production loaded `xingqiao-sub2api:upstream-0.1.166` with platform image ID
`sha256:e146027d59ab96c40d9ef12eea3943446a22b32e7aba918b85313909efce4ccf`.
The server-side image platform, labels, size, and binary version matched the
qualified local platform image. All local and production transfer artifacts
were removed after verification.

The running `sub2api-sub2api-1` container remained:

- image ID:
  `sha256:728e722fc447211bff893b4ad893f7418aa96c58d762648a2103b125e9481460`
  (qualified `0.1.165`);
- status `running`, health `healthy`, restart count 0;
- original start time `2026-07-26T09:46:56.335640223Z`.

`sub2api-updater.service` remained active and the public `/health` endpoint
returned `{"status":"ok"}`. The run did not call
`/api/v1/admin/system/update`, edit production Compose, recreate a production
container, or mutate application data. The next action is the administrator's
manual update click.
