# 当前生产交付决策总表

**状态：** 已确认

**确认日期：** 2026-08-02

**唯一协调任务：** 当前“全局进度、账号评分、账务与飞书联动”任务

**合同起始基线：** `origin/main@f560400f26b3489cdda2a7f214a3001b8e6a1a38`

**任务执行基线：** 由当前协调任务逐项下发；后一个任务只能继承前一个任务已推送、已部署、已验证的最新远端基线。

本文件保存当前仍有效的产品决定、纠正项、任务边界和完成标准。聊天记录是来源，本文是执行入口；任何任务恢复、上下文压缩或换窗口后，都必须先重新读取本文，再读取自己的生产计划。

## 2026-08-04 账号监控 V3 追加裁决

账号监控卡片的最新职责、数据、交互与 1:1 视觉要求，以 [账号监控 V3 不可漂移验收合同](account-monitor-v3-acceptance-contract.md) 为最高优先级执行入口。该合同覆盖本文件下方与它冲突的旧决定，尤其包括：

- `/admin/accounts/monitor` 当前只承接账号服务质量、评分、排名与调度信息。
- 营收、用户计费、利润、账务、对账、流水、异常处理和运营汇总迁往后续独立“营收”页面，不得在本轮账号监控页恢复。
- 已确认 V3 截图与交互原型是完整视觉真值；账号卡片的近期探测、窗口调用折叠和检查/刷新页脚均为必需结构，不得为了“收敛”删除。
- 当上游（包括常见 newapi）没有原生倍率时，账号卡允许录入/修改手工倍率；写入 `rate_multiplier` 与 `rate_multiplier_policy=manual_override`，不得让后续托管探测覆盖该值。
- 任何任务恢复、上下文压缩、子代理实施、独立审查或生产部署前，都必须先读取该合同；发现冲突时停止并由当前协调任务裁决。

## 来源任务

- `019fbef4-3c0c-7441-9b3a-3d03229eee89`：完成项目收尾与生产部署。
- `019fbe0c-acd9-7da3-abfd-35e71ad71879`：讨论账号分组监控与账务优化。
- `019fbda4-19b9-7462-927e-c5260071bf10`：接线实时上游账单采集。

新任务不得只依赖标题或摘要；必须调用 `read_thread` 读取上述任务的完整历史，并以本文件列出的“已确认/已纠正”状态解决冲突。

## 已确认产品决定

### 唯一管理入口

- 管理员账号监控唯一入口是 `/admin/accounts/monitor`。
- 不恢复 relay-ops 自建监控页面、运维页面或浏览器控制面。
- 评分与服务健康在 `/admin/accounts/monitor` 展示或操作；真实账务、对账和其他经营内容按 2026-08-04 追加裁决迁往独立“营收”页面。
- 公共 `/pricing` 是面向访问者的价格页面，不是第二个管理员监控入口。

### relay-ops 边界

- 本轮保留 relay-ops 作为不可见的内部后台服务，负责定时采集、逐笔对账、每日结账、价格采集和飞书出站。
- 浏览器关闭后仍必须持续运行的任务不能由 Vue 页面承担。
- relay-ops 不再拥有独立 UI；受保护后台接口服务对应的 Sub2API 管理页面和内部调度，不要求经营内容继续放在账号监控页。
- 本轮不把 relay-ops 全量迁入 Sub2API worker；待三轮生产闭环稳定后再单独评估服务合并。

### 账号质量评分

- 每个分组独立保存四项评分权重，默认和恢复默认均为：成本优势 15、成功率 45、首包时间 20、总耗时 20。
- 四项必须为非负整数且合计 100；只提供直接配置、保存和恢复默认，不提供预设模板。
- 评分只影响 `/admin/accounts/monitor` 的状态和组内排名，不写入调度器。
- `accounts.priority` 是唯一可编辑的实际调度优先级；卡片修改后立即刷新。
- 成本优势是账号倍率相对当前分组倍率的安全余量，不等于真实利润率。
- 同一账号在不同分组可以有不同成本优势、评分和排名。
- 分组 Tab 按原生分组倍率从高到低；同倍率保持原生稳定顺序，不写排序字段。

### 监控与分组语义

- 账号服务健康在 `/admin/accounts/monitor` 支持全站、分组、账号三个层级；经营结果在后续独立“营收”页面支持相应层级，两者不得重新混排到账号监控页。
- 分组未向任何有效用户开放时显示“已关闭”，只提示一次，不持续报告“无可用账号”。
- 已开放分组没有可用账号，或存在真实用户请求失败时，仍按故障处理。
- 组内评分使用所选 `24h`、`7d` 或 `30d` 的真实请求窗口；真实请求少于 3 次时才允许使用同期探测补充服务质量证据，探测不得计入真实请求数、失败数或基础成本。

### 全站记账与对账

- 范围是全站所有启用且具备账单能力的账号，不按 Wawazz、Neko 或任何供应商名称限定。
- 定时扫描和日结使用全量账号范围；每个可计费账号必须具备合法只读账单授权和明确的账单账号映射。
- 上游真实扣费只来自真实上游交易/快照；不得用倍率、旧 `today_stats` 或前端估算冒充。
- 用户实际计费使用本站真实请求扣费；全站按唯一对账请求计算一次。
- 分组按请求发生时的 `group_id` 归属；未归属请求只进入全站并解释差额。
- 后续独立“营收”页面展示：用户实际计费、上游真实扣费、账号利润、利润率、成本覆盖率、待对账数量和历史累计；账号监控页不得展示这些字段。
- 对账未闭合时显示“待对账”或“对账异常”，不得展示伪造的确定利润。

### 飞书联动

- 飞书只保留主动告警、恢复通知和日报，不恢复入站命令或写控制。
- 最新成功结果不能被历史失败覆盖；最新成功后不能继承历史 `balance_exhausted` 等错误码。
- 所有飞书管理链接统一指向 `/admin/accounts/monitor`。
- 原生 P0/P1 Bridge 本轮不扩大；现有链路满足告警和恢复后即停止。

### 中文展示

- 所有页面、面向人的报告和任务汇报使用中文业务名称。
- 数据库、API 和代码字段可以保留英文，但只能出现在代码、SQL 或技术附录。
- 五类核心数据统一称为：账单授权会话、上游成本快照、对账请求明细、对账执行记录、每日账务快照。

## 已纠正或废止的旧口径

- “账号监控入口必须并入 `/admin/accounts`”已纠正；正确入口是 `/admin/accounts/monitor`。
- “账单任务只处理 Wawazz”已废止；正确范围是全站所有可计费账号。
- “账号监控页同时展示经营结果、账务和对账”已废止；正确边界是账号质量留在 `/admin/accounts/monitor`，经营内容迁往后续独立“营收”页面。
- “纸面利润”已废止；后续营收页面统一使用“账号利润”。
- relay-ops 数据库里存在历史上游登记，不等于该供应商仍是当前生产范围。
- “代码已合并/候选已构建”不等于完成；必须同时具备推送、生产部署和线上生效证据。
- 自建 relay-ops 页面已退役，但 relay-ops 内部后台任务当前仍保留。

## 小步快跑执行纪律

- 每个生产任务以 30 分钟为目标节奏，但完成更新、生产部署和线上验证优先；不得因超过目标时间而停止必要修复、跳过门禁或制造完成结论。
- 任务只允许一个生产结果；不接管下一任务，不修复无关测试，不重构架构。
- 0-5 分钟：读取本合同、计划、生产基线和发布锁，只读预检。
- 5-12 分钟：运行最小目标验证或复用绑定同一代码树的合格证据。
- 12-22 分钟：部署唯一目标；脚本必须带总 deadline 和阶段超时。
- 22-28 分钟：执行线上 API、页面、数据或飞书验收。
- 28-30 分钟：记录推送 SHA、运行镜像、验收结果和回滚点；更新协调任务。
- 任一阶段超出目标节奏时，继续限制在当前任务范围内完成修复；只有真实外部阻断或继续操作会扩大生产风险时才暂停。未完成就保持“进行中”，不得以阻断报告冒充完成。

## 顺序与所有权

1. 账号监控 V3 已在生产收口；后续基线只可从其已部署远端事实派生。
2. 全站账务运行时代码生产发布。
3. 全站账单授权、映射与真实非零闭环。
4. 独立 `/admin/revenue` 营收页面生产发布。
5. Monitor 当前状态与飞书历史错误码生产发布。
6. OpenAI 实际响应模型审计与管理端展示生产发布。

- 后续生产单元顺序执行；后一个单元只从前一个已推送、已部署、已验证且经用户验收的基线派生。
- 六阶段收口以 `docs/superpowers/plans/2026-08-05-six-stage-production-closure-deployment-units.md` 为执行入口；生产单元必须串行，任何一次部署或生产配置激活完成后立即写为“等待用户验收”。
- 用户未明确验收前，不得将该部署单元标记完成、派发下一部署单元或以本地/离线验证替代线上生效证据。
- 任一时刻只允许一个新生产实施任务运行；三个来源任务冻结为只读上下文，不再自行修改代码、合并或部署。
- 每个生产实施任务内部应派生 subagent：实现者负责单一代码范围，独立复审者检查需求、测试和生产证据，结束前再做一次全分支终审。
- 只读测试、根因分析和证据复核可以由 subagent 并行执行；同一文件写入、分支提交和生产发布必须串行，且各自只有一个明确所有者。
- 当前协调任务是唯一允许修改 `docs/project/project-progress.md`、合并 `origin/main` 和宣布生产完成的任务。
- 实施任务只修改自己的代码、测试、验证报告和任务计划；不得删除或合并其他 worktree。
- 每个实施任务使用独立 Codex worktree，结束时提交并向协调任务交付，不直接覆盖共享主工作区。

## 完成证据

每项完成必须同时提供：

1. 已推送的提交 SHA。
2. 生产实际运行的不可变镜像、容器或提交身份。
3. 对应线上页面、API、真实数据或飞书告警/恢复验证。

缺少任意一项，状态保持“进行中”。
