# OPT-003 v0.2 补漏修复本地复核

日期：2026-09-26。状态：候选代码与本地验证完成，未提交、未推送、未部署，不代表测试站或 v0.2 全版本验收通过。

## 依据与范围

- 需求原文：`/Users/awen/Documents/ChatGPT/星桥AI Link-mvp/docs/project/tasks/OPT-003-user-end-v0.2.md`。
- 支付关闭态按用户后来确认的方案执行：侧栏进入兑换页；保留禁用的充值标签与不可用提示，隐藏订单入口。该确认覆盖原文中完全隐藏充值入口的旧表述。
- 本次补漏范围为支付关闭态入口、创建及编辑密钥弹窗字阶、共享品牌下拉。未重新审计原文所有业务范围，也未将原文历史检查点当作当前运行状态。
- 候选工作区：`/Users/awen/.codex/worktrees/opt-003-v02/星桥测试服`，分支 `codex/opt-003-v02`。
- 基线 commit：`734b51ff579444984bb447ebfc33a9f3ac635dd6`；tree：`d451b62e5dbb741f990d1aafc956d36759922114`。以下修复均仍在未提交工作区。

## 修复与需求对照

| 项目 | 本次修复 | 验证结果 |
| --- | --- | --- |
| 支付关闭态入口 | 侧栏跳转 `/redeem`；兑换页充值标签禁用，显示“充值暂不可用，仍可使用兑换码。”，隐藏订单入口 | 本地完整路由核对关闭态；独立启用态会话中侧栏和充值标签恢复 `/purchase`，订单入口为 `/orders` |
| 创建密钥弹窗主题 | 显式启用品牌弹窗，不依赖普通用户主题注入，管理员访问用户页时也适用 | 普通用户及管理员模拟会话均得到标题 20px/600、七个字段标题 12px/500 |
| 创建与编辑字阶 | 输入内容 14px/400、辅助说明 11px/400；补齐编辑弹窗字段样式及线路选择触发器字重 | 浏览器读取计算样式确认；修改限定在密钥弹窗，不扩大到其他管理弹窗 |
| 下拉浮层不透明 | 缺少继承变量时使用现有品牌色默认值，存在主题变量时继续采用实际变量 | 稳定截图中背景为 rgb(9, 26, 43)，opacity 为 1；文字、边框、搜索及选中态可见 |
| 下拉键盘操作 | 无搜索菜单打开后获得焦点；方向键加 Enter 可以选择；Escape 阻止向外层弹窗冒泡；Tab 恢复正常焦点顺序 | 新增两个交互回归；实际编辑弹窗选中“禁用”后焦点回到按钮，Escape 只关闭菜单，Tab 移到后续控件 |
| 倍率与窄屏 | 保持现有共享倍率解析，本次不改倍率接口与算法 | 用户专属倍率显示 `0.12倍率`；缺少专属倍率时回退分组值 `0.5倍率`；390px 下菜单左右为 45px/345px，页面无横向溢出 |

## 自动化验证

在候选前端目录执行：

- `./node_modules/.bin/vitest run --reporter=dot`：344 个测试文件通过，2457 项通过，17 项跳过，0 失败。包含本次支付入口、共享下拉、弹窗及既有 i18n 测试。
- `./node_modules/.bin/vue-tsc --noEmit`：通过。
- `./node_modules/.bin/vite build`：通过。
- `git diff --check`：通过。
- Impeccable 对八个变更界面文件检查：报告五条 `KeysView.vue` 原有灰色文字搭配彩色背景提示，位于未修改的 389、398、409、410、428 行；本次新增代码未命中该检查。未为消除旧提示修改无关列表样式。

本次修改八个源文件、六个测试文件；另新增本文档。未修改依赖锁文件、后端、数据库、支付配置或路由支付守卫。

## 浏览器验证边界

使用本地 Vite 实际页面路由及隔离的 Playwright 浏览器，桌面 1440px、窄屏 390px。业务接口全部由本地模拟响应拦截，外部请求被阻止，不使用真实账号凭据，不创建真实密钥、订单或上游请求。

已核对普通用户及管理员访问密钥页面的创建/编辑弹窗、支付开关两种导航状态、倍率优先和回退、下拉搜索/选择/方向键/Enter/Escape/Tab、浮层背景及窄屏边界。最终模拟页面没有未捕获脚本异常。

本地模拟验证不能替代部署后真实登录态、真实接口数据、失败重试、真实线路抽检和全站金额/颜色/标题逐项验收。真实支付保持未验收。

## 运行环境与下一步

- 独立测试站的只读发布记录仍为上述基线 commit/tree，记录 `result=succeeded`、`rolled_back=false`。这说明旧部署成功，不包含当前未提交修复。
- 当前候选 HEAD、本地 `main`、本地 `origin/main` 指向相同基线；本轮未 fetch，不能据此断言远端此刻没有新提交。
- 根目录有既有修改及未跟踪文件，未清理、覆盖或纳入本次修复。发布前必须保留这些内容并由发布总控处理根目录状态。
- 主站本轮未访问或操作，commit/tree 未核对，不能声称双站一致。
- 待明确授权提交、整合、推送及部署独立测试站后，按根目录干净 `main` 与已推送 `origin/main` commit/tree 完全一致的门禁生成制品；不能从候选 worktree 直接发布。
- 不可推广内容包括本地模拟数据、测试浏览器状态、未审阅的根目录既有内容以及未提交工作区制品。
- 本轮未进行环境切换或回滚。后续发布须保留当时运行版本的回滚信息并执行发布脚本预检；若预检要求停机，应按项目规则先取得明确授权。

下一步为审阅本次补漏改动并确认整合发布权限，再部署独立测试站完成真实页面验收。当前不标记版本 DONE。

## 发布授权追加

2026-09-26，用户对“提交这些修复、合入并推送 main，然后部署独立测试站并进行真实页面验收”明确回复“确认”。以上本地复核结果作为发布候选依据；发布结果另行记录。本次授权不包括主站发布、真实支付或新业务规则调整。
