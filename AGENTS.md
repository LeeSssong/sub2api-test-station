# 星桥测试服项目规则

## 默认读取策略

- 新任务默认只读取本文件、用户当前指令及任务直接涉及的代码或文档；不得启动时完整读取大型队列、总账、历史交接或全部发布记录。
- 普通查询、诊断和独立开发不读取 `docs/project/native-sub-task-package-queue.md`、`docs/project/project-progress.md` 或发布专项文档。
- 领取编号任务包时，用任务编号或标题在任务队列和项目总账中定位，只读取该任务条目、当前状态及直接依赖；需要流程细节时再读取 `docs/project/native-sub-incremental-delivery-constraints.md` 的相关章节。
- 涉及测试站登录、日志、运维、部署或主站发布时，才完整读取 `docs/project/acceptance-station-global-constraints.md`，并核对当前服务器和 Git 状态。
- 发布总控只读取队列、总账中的当前活动条目、领先候选和最新发布记录；历史条目按需检索，不得整文件加载。
- 大任务跨窗口继续时读取当前任务自己的规格、计划、交接或 `TASK-CONTEXT.md`；不得把其他任务历史一并载入。

## 开发与工作区边界

- 当前用户指令优先。查询和诊断默认只读；未经明确要求不修改、提交、推送、部署或执行外部操作。
- 修改前检查当前分支、工作树和相关 worktree，保留已有改动；禁止用覆盖、删除、强制 reset 等方式清理来源不明的内容。
- 功能任务默认从最新 `origin/main` 创建独立 `codex/<task-name>` 分支和 worktree；一个 worktree 同时只能有一个写入者。
- 优先复用 Sub2API 原生代码、API、页面、数据口径和发布链；没有证据证明必要时，不新增平行控制面、账务源或管理入口。
- 计划内功能以“实现完成并通过直接相关测试”为候选完成门槛；不为形式扩大测试或复审。未经推送、部署和线上验证不得标记 `DONE`。
- 只有发布总控可修改根目录 `main`、任务队列、项目总账、发布证据和生产状态记录；同时只能有一个任务处于整合、部署或线上验证阶段。
- 冻结的任务、分支和 worktree 只作只读证据，未经明确解冻不得继续写入、合并、部署或清理。

## 发布与环境硬门禁

- 测试站、主站及其他环境的部署制品只能从根目录干净的 `main` 生成；必须确认非 detached HEAD，且 `HEAD` commit/tree 与已推送的 `origin/main` 完全一致。
- 禁止从功能 worktree、候选分支、服务器目录、容器文件、运行镜像或 Docker volume 直接推广；Git 中可审计的 commit 是唯一代码来源。
- 发布准备与部署不得使用 GitHub Actions，只使用已审查的本地或宿主脚本链。
- 主站发布只接受三种明确授权：`测试站验收通过，部署主站`、`快速部署到主站`、`快速部署主站，不同步验收站`。普通“继续”“上线”“部署一下”等表述不构成授权。
- 第三种授权必须记录主站与测试站 commit/tree 差异，不得声称两边一致。发布预检返回 `downtime_required=true` 时，必须在停服、迁移、重启或切换前停止并取得明确授权。
- 测试站与主站的数据、账号、账务、缓存、上传文件、日志和凭据完全隔离；只允许推广已审核代码、migration、配置变化及可追溯静态制品。
- 密码、token、私钥、API Key、支付/上游/通知凭据只能从受保护位置读取，不得打印、复制到仓库、聊天、规格、证据或普通日志。
- 官方 Sub2API 更新可跳过额外功能测试和专项验收，但不得绕过冲突解决、根 `main` 来源、推送、发布授权、迁移/停机保护、运行就绪和失败回滚。

## 权威文档路由

- 任务包流程、状态机、原生优先和最小验证：`docs/project/native-sub-incremental-delivery-constraints.md`
- 当前任务队列与任务条目：`docs/project/native-sub-task-package-queue.md`
- 项目进度与历史状态：`docs/project/project-progress.md`
- 测试站身份、凭据路径、运维命令、发布授权和双站同步：`docs/project/acceptance-station-global-constraints.md`
- 当前用户指令和实际代码、Git、服务器状态高于历史记录；发现冲突时先报告，不按旧记录推断。

## 交付要求

- 未经实际验证不得声称完成。无法验证时列出未验证项。
- 涉及代码推广或环境操作时，最终报告须包含：改动与 commit、不可推广内容、主站和测试站 commit/tree、一致性、测试、发布、回滚和未解决问题。
