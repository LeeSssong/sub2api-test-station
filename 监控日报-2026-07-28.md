# 星桥 AI 监控日报

- 报告日期：2026-07-28（Asia/Shanghai）
- 数据来源：站内「使用记录 / 错误请求」「运维监控 / 告警事件」「账号管理」「操作日志」
- 统计口径：按 2026-07-28 00:00:00 至 23:59:59 的站内记录统计；持续时间为首条至末条同类错误的可观测窗口，不等同于百分之百不可用时长

## 一、日报摘要

7 月 28 日共处理 3,243 个请求，产生 257 条错误记录，原始错误率为 7.92%。剔除客户端请求体错误、用户余额限制和分组权限配置后，确认与服务可用性有关的错误为 233 条，占当日请求的 7.18%。

当日服务问题高度集中：20:27:21 至 20:35:37 的上游账号池不可用事件产生 219 条服务错误，占服务类错误的 94.0%。其余为一次上游账号限流引发的短时容量不足、一次孤立的上游 503，以及人工关闭两个 TK 调度账号后剩余账号发生的短时上游失败。

站内应用、数据库、Redis、CPU 和内存未发现资源瓶颈证据。主要问题在上游账号池容量、共同故障域和调度保护机制，而不是本机基础设施。

## 二、核心指标

| 指标 | 数值 |
| --- | ---: |
| 总请求数 | 3,243 |
| 总 Token | 407.33M |
| 平均耗时 | 13.88s |
| 全部错误记录 | 257 |
| 原始错误率 | 7.92% |
| 服务类错误 | 233 |
| 服务类错误率 | 7.18% |
| 非服务类错误 | 24 |
| 最大单次事件 | 219 条服务错误 |

## 三、服务事件明细

| 级别 | 时间窗口 | 持续时间 | 错误数 | 主要错误 | 原因判断 | 恢复手段 |
| --- | --- | ---: | ---: | --- | --- | --- |
| P1 | 02:12:29 - 02:17:38 | 5分09秒 | 10 | 429 `触发限流`；503 `No available OpenAI accounts` | `gelato_04...` 账号触发上游限流并退出调度，短时间内账号池没有可用 OpenAI 账号 | 系统自动隔离限流账号并切换其他账号；该账号当前仍处于限流状态、调度关闭，预计 2026-08-04 12:38:49 自动恢复 |
| P2 | 18:43:16 | 单点事件，<1分钟 | 1 | 503 `Upstream service temporarily unavailable` | `gelato_04...` 上游出现一次瞬时 503 | 后续未出现连续同类错误，按自动重试/路由切换恢复处理 |
| P0 | 20:27:21 - 20:35:37 | 8分16秒 | 219 | 403 `Upstream access forbidden`；503 `Service temporarily unavailable`；503 `No available OpenAI accounts`；502 上游余额不足 | `Plus-TK-极速` 先返回访问拒绝，随后路由池大量返回无可用账号；同时 TK 上游出现余额不足，且 `gelato_04...` 已在长期限流，账号池缺少独立可用备份 | 站内未发现该窗口内的人工调度操作；同类错误在 20:35:37 后停止，判断为上游/路由自动恢复。21:52 后新增 OAuth Plus 账号属于事后容量补强，不是本次即时恢复动作 |
| P1 | 22:04:35 - 22:07:08 | 2分33秒 | 3 | 400 `Upstream request failed` | 管理员在 22:04:26、22:04:27 分别关闭账号 #31、#20 的调度，流量集中到 `VescoviKluge819...`，该上游随后连续失败 3 次 | 剩余 OAuth 上游自行恢复，22:07:08 后未再出现同类错误；当前 `VescoviKluge819...` 状态正常，TK 账号后续也已恢复调度 |

### P0 事件错误构成

| 错误 | 数量 | 时间范围 |
| --- | ---: | --- |
| 路由 503 `Service temporarily unavailable` | 197 | 20:27:23 - 20:34:02 |
| 503 `No available OpenAI accounts` | 18 | 20:28:09 - 20:34:21 |
| 502 上游账户余额不足 | 3 | 20:35:35 - 20:35:37 |
| 403 `Upstream access forbidden` | 1 | 20:27:21 |

## 四、非服务类错误

以下 24 条记录不应计入服务 SLA，但当前会推高统一错误率并触发噪声告警：

| 类型 | 数量 | 说明 |
| --- | ---: | --- |
| 400 `Failed to read request body` | 17 | 客户端请求体不完整或连接中断 |
| 403 `insufficient balance` | 6 | 用户侧余额/订阅限制 |
| 403 `Image generation is not enabled for this group` | 1 | 分组权限配置限制 |

23:28 至 23:55 的 P0/P1 错误率、成功率告警主要由 400 请求体错误触发，告警持续 1 至 5 分钟并自动恢复。这些告警不代表站点服务故障。

## 五、根因分析

1. 上游账号池存在共同故障域。#20 和 #31 均来自同一 TK 上游；当供应商访问权限或余额异常时，两条账号不能构成真正冗余。
2. 可用容量过薄。`gelato_04...` 被 429 限流后，账号池长时间少一个主要容量来源；P0 事件中剩余 TK 上游又同时失效，导致路由层直接返回 503。
3. 调度缺少最小容量保护。22:04 连续关闭两个 TK 账号后，系统允许可用池缩减到单一 OAuth 上游，随后该账号发生短时失败。
4. 错误分类与告警口径混杂。客户端 400、用户余额、权限配置和真实 5xx 服务故障进入同一错误率告警，造成 P0/P1 噪声。
5. 路由错误缺少可诊断维度。大量 503 记录的账号字段为 `-`，无法直接从单条日志判断被排除账号及排除原因。

## 六、后续优化建议

### P0：立即处理

1. 为每个生产分组设置“至少 2 个健康账号、至少 2 个独立上游”的硬性门槛；同一供应商的多个 Key 只算一个故障域。
2. 给调度变更增加容量保护：关闭账号前预检剩余健康容量，低于门槛时阻止操作并显示受影响分组。
3. 对 403、429、余额不足建立账号级熔断：首个确定性错误立即隔离账号，后台探测恢复后再以小流量放回，避免用户请求承担探测成本。
4. 增加上游余额、订阅到期、429 恢复时间和健康账号数量告警；健康账号少于 2 个时直接触发 P0。
5. 修复通知链路。当前告警事件的邮件状态均显示“已忽略”，需要确保 P0/P1 至少有一个可达通知渠道和明确值班人。

### P1：本周完成

1. 将告警拆成 `service_error_rate`、`client_error_rate`、`business_limit_rate`，SLA 和 P0 只统计服务端、路由和上游故障。
2. `/models` 使用带过期时间的缓存与 stale-while-revalidate；上游余额查询失败时返回最近一次有效模型清单，避免模型发现接口因供应商余额问题返回 502。
3. 对幂等请求增加指数退避和随机抖动，并限制重试预算，避免账号池故障时 197 条重复 503 形成重试放大。
4. 相同错误按“错误签名 + 分组 + 上游 + 时间窗”聚合成一个事件，同时保留受影响请求数、用户数和首末时间。
5. 给新增或重新启用账号设置灰度期，先通过健康检查和少量流量验证，再进入全量调度。

### P2：持续改进

1. 在路由日志中记录候选账号、排除原因、最终路由结果和故障域，避免账号字段为空。
2. 建立账号池容量看板：健康账号数、可调度容量、限流账号数、供应商余额和近 5 分钟切换次数。
3. 为“无可用账号”“上游余额不足”“关闭最后健康账号”编写自动化处置 Runbook，并在事件结束后自动生成时间线。

## 七、建议验收标准

- 任一上游供应商全部失效时，生产分组仍有另一个独立上游可接管。
- 关闭账号后，系统不允许健康账号数低于 2。
- 403/429/余额不足出现后 10 秒内完成账号隔离，且不再向用户重复暴露同一确定性错误。
- 客户端 400 和用户余额不足不再触发服务 P0/P1。
- P0 告警在 1 分钟内送达值班人，并能从告警直接定位分组、供应商和受影响账号。
