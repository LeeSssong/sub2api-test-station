# T133 账号监控连接测试模型入口与主动探测成功率修复实施计划

## 基线与边界

- 基线：`main@3c7b6530e4a8a6cf9d861cd1852b05ed37736d39`
- 候选分支：`codex/t133-account-monitor-probe-rate-inline-model`
- 候选 worktree：`.worktrees/t133-account-monitor-probe-rate-inline-model`
- 无迁移、无配置、无历史回填、无生产业务数据写入。
- 只改账号监控统一聚合、分组投影和账号卡片；不改探测调度、调度资格或账务。

## Task 1：建立候选和 RED 后端合同

1. 从最新干净且已推送的根 `main` 创建独立 worktree。
2. 在仓储测试中加入失败探测进入分母、同桶只取一个终态、真实请求优先及全局/分组一致断言。
3. 在服务测试中加入分组投影不得用旧窗口零值覆盖统一成功率的断言。
4. 运行聚焦测试并记录预期失败，确认失败来自目标缺陷。

## Task 2：实现统一探测终态聚合

1. 修改全局与分组统一 SQL，使明确成功和失败探测终态都能作为无真实请求桶的单一证据。
2. 保持真实请求同桶优先和最新终态去重。
3. 调整服务分组投影，停止旧窗口聚合覆盖统一请求数、成功数、失败数、成功率和 TTFT。
4. 运行 Task 1 聚焦测试至 GREEN，并执行格式检查。

## Task 3：建立 RED 前端合同并实现内联选择器

1. 为 `AccountMonitorCard` 增加选择器布局、模型加载、即时保存、保存中禁用和失败回滚测试，先确认 RED。
2. 复用现有模型列表与账号保存 API，在模型结果/勾选项旁加入紧凑原生 `select`。
3. 保留现有检测弹窗和其中设置，避免两个入口状态漂移。
4. 运行组件测试至 GREEN。

## Task 4：候选验证与提交

1. 运行账号监控仓储/服务聚焦测试、前端组件测试、前端 typecheck、`go build ./cmd/server` 和 `git diff --check`。
2. 审查 `git diff --stat`、`git diff --name-status`、迁移/配置/敏感文件变化。
3. 更新候选规格/计划或交接所需状态，提交候选并记录 SHA。

## Task 5：根 main 整合、推送和主站发布

1. 确认唯一发布车道、根 `main` 无漂移且候选范围清晰，合并候选。
2. 在根 `main` 重新运行直接相关回归、构建、类型检查和差异检查。
3. 更新总账/队列，提交并推送 `origin/main`，确认 commit/tree 完全一致且根工作树干净。
4. 执行主站发布预检；`downtime_required=true` 时立即停止并请求授权。
5. `downtime_required=false` 时按用户 C 路径从根 `main` 发布主站，不部署验收站。
6. 验证主站 source commit/tree、发布记录、三项健康探针、账号监控 API 和目标 UI；记录验收站当前 commit/tree 与明确版本差异。
7. 生成 0600 发布证据和可恢复 bundle，确认可恢复后清理候选 worktree/本地分支，将任务标记 `DONE`。

## 计划自审与批准

计划严格遵循“先 RED、后最小实现、候选验证、根 main 串行整合、推送后发布”的顺序，且为 `downtime_required=true` 保留人工门禁。没有未决产品问题或范围扩张。发布总控依据既有代审授权批准本计划执行。
