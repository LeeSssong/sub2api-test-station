# SDD ledger — plan: docs/superpowers/plans/2026-08-04-account-monitor-card-production-implementation-plan.md

Started from `278f693cb` on branch `codex/account-monitor-completion`.
Global status: implementation in progress; stop before push/deployment.
Task 1: dispatched at base `278f693cb` to `/root/prod_task1_procurement`.
Task 1: implementation commit `44e24ae9b`; report written; task review dispatched.
Task 1: review found 1 Critical compile failure (repository uses `CNY` while generated Ent exposes `Cny`); fix round 1/5 started.
Task 1: fix round 1/5 commit `90201d3a0` (1 addressed, scoped re-review dispatched).
Task 1: fix round 1/5 (1 addressed, 0 open; commit `90201d3a0`; independent scoped re-review PASS).
Task 1: complete (commits `278f693cb..90201d3a0`, review clean).
Task 2: dispatched at base `90201d3a0` to a fresh independent Codex implementer (CLI session handle `30799`).
Task 2: implementation commit `91cf75256`; focused tests and service/repository/admin-handler package suites passed; independent review completed.
Task 2: review found 1 Critical (required `/accounts/monitor` route missing), 1 Important (probe fallback lacked selected-window upper bound), and 2 Minor test coverage gaps; fix round 1/5 dispatched.
Task 2: fix round 1/5 commit `f04a5398c` addresses all four review findings; focused and route suites pass; full regression still exposes a Task 1 procurement-column SQL mock fixture mismatch, assigned back to Task 1 cleanup before Task 2 re-review.
Task 1: fixture cleanup commit `92a06daa6` aligns both `dbaccount.Columns` test helpers with the two procurement columns; named RED/GREEN tests and full repository package pass; independent cleanup review found no issues.
Task 2: full four-package regression and scoped re-review pending after Task 1 fixture cleanup.
Task 2: full four-package regression passed at `92a06daa6`; independent scoped re-review of `f04a5398c` found 0 Critical, 0 Important, 0 Minor and returned PASS.
Task 2: complete (commits `91cf75256`, `f04a5398c`, plus Task 1 fixture cleanup `92a06daa6`; review clean).
Task 3: preparing fresh implementer dispatch at base `92a06daa6`.
Task 3: implementation commit `be9257c2f`; RED/GREEN, full affected-package tests, Wire compile, vet, and server build passed.
Task 3: independent review found no defects and one exact-200 boundary coverage gap; coverage added and scoped re-review returned no findings.
Task 3: complete (commits `92a06daa6..be9257c2f`, review clean).
Task 4: preparing fresh implementer dispatch at base `be9257c2f`.
Task 4: implementation commit `2ea3ef167`; focused card tests and frontend typecheck passed; independent task review completed.
Task 4: review found 1 Important (backend monitor projection omits procurement amount/effective time/native expiry) and 1 Minor (procurement save/clear rejection coverage); parent completion wiring was reclassified as Task 5 integration scope. Fix round 1/5 started.
Task 4: fix round 1/5 commit `cd6504358` projects procurement fields and covers save/clear rejection behavior; scoped re-review dispatched.
Task 4: fix round 1/5 (2 addressed, 0 open; commit `cd6504358`; independent scoped re-review PASS).
Task 4: complete (commits `be9257c2f..cd6504358`, review clean).
Task 5: preparing fresh implementer dispatch at base `cd6504358`.
Task 5: implementation commit `ae3d591a9`; 17 focused tests, frontend typecheck, and production build passed; independent task review preparing.
Task 5: review found 1 Important (response range not verified before committing selected snapshot); fix round 1/5 started.
Task 5: minor (deferred): plan names `pnpm type-check`, while repository's supported script is `pnpm typecheck`; runtime verification used the supported command successfully.
Task 5: fix round 1/5 commit `6b6c8ff20` rejects mismatched/missing response ranges before committing the selected snapshot; 18 focused tests and frontend typecheck passed; scoped re-review preparing.
Task 5: fix round 1/5 (1 addressed, 0 open; commit `6b6c8ff20`; independent scoped re-review PASS).
Task 5: complete (commits `cd6504358..6b6c8ff20`, review clean; 1 deferred Minor).
Task 6: user rejected the visual result after local production-component QA; root cause is `2ea3ef167` removing the service-only lower card modules while its tests covered only the simplified upper card. Fresh screenshot-faithful implementer preparing at base `6b6c8ff20`.
Task 6: dispatched at base `6b6c8ff20` to fresh Codex implementer session `019fcb32-6afa-70b0-a324-1b753720f001`; highest-priority contract is `docs/project/account-monitor-v3-acceptance-contract.md`.
Task 6: context recovery audit completed after user raised drift risk. Coordinator re-read source task `019fc276-948f-7600-9d60-dc75e1027b12`, acceptance contract, active delivery contract, implementation plan, ledger, git state, and desktop/mobile visual truth. Contract now records decision priority and mandatory recovery protocol; conflicting global-ledger entries that kept economics inside account monitor were explicitly superseded.
Task 6: same-state visual QA remains open. At 1440x1000 the implementation is structurally close but still differs from the confirmed source: group-summary rows are too short, card header/score band are too compressed, TTFT/latency values wrap, and the QA projection currently renders zero probe results instead of the required 24-point example. At 390x844 the same compressed summary shifts the first card upward. These are blocking P1/P2 findings for the independent review/fix loop; component tests and build success do not close them.
Task 6: independent review of `e85dad164` returned BLOCK with 7 Important findings: missing `ListTimelines` in `ListWindow`; missing valid all-site score/rank; compressed dimensions and 16px card radius; wrapped thousand-separated latency values; truncated mobile identity; long cutoff/non-stacking mobile footer; and unrealistic tests/QA fixtures plus `全部` copy drift.
Task 6: context recovery repeated after user explicitly questioned context loss. Coordinator paginated source task user decisions, reread the acceptance contract, active delivery contract, production plan, SDD ledger, Git state, current review, and desktop/mobile truth plus implementation captures. Fix round 1 brief: `.superpowers/sdd/2026-08-04-account-monitor-card-production-implementation-plan/task-6-fix-1-brief.md`.
Task 6: fix round 1/5 preparing fresh implementer at base `e85dad164`; push, deploy, production access, and completion status remain forbidden.
Task 6: fix round 1/5 (6 addressed, 1 open; commits `a34342740`, `4f6fc8e11`). Scoped re-review kept finding 7 open: the new tests do not exercise top-level all-site equal-score account-ID tie-breaking, and the frontend fixture uses duplicate ranks that the production algorithm cannot return.
Task 6: fix round 2/5 resuming the original implementer; scope is only a top-level equal-score stable-ranking regression plus API-shaped unique sequential frontend ranks.
Task 6: fix round 2/5 (1 addressed, 0 open; commits `8d690a6e2`, `539649308`). Scoped independent re-review returned ADDRESSED with 0 Critical, 0 Important, and 0 Minor; top-level all-site equal-score account-ID ordering, continuous ranks, unranked-last behavior, and API-shaped frontend fixtures are now covered. Same-state desktop/mobile visual QA and final whole-branch review remain open; push, deployment, production access, and completion status remain forbidden.
Task 6: same-state visual QA after fix round 2 captured the selected `GPT-PLUS-内测` state at 1440x1000 and 390x844. Desktop exact gates pass (82px summary, 121px score cell, 8px radius, 612px two-column cards, no horizontal overflow, 24 probe bars per card, zero console warnings/errors). Mobile remains blocked by two P2 differences against the confirmed source: production uses 20px horizontal/32px top page padding instead of 12px/22px, and the selected group tab shows the browser default orange focus outline after mouse click instead of the prototype's teal underline-only state. Fix round 3/5 is dispatched with these two issues only; visual QA must be rerun before any PASS.
Task 6: fix round 3/5 implementation commit `de2ee4430` adds the 12px/22px mobile shell overrides and removes default tab focus outline while retaining keyboard `focus-visible` feedback; focused view tests report 12/12 and frontend typecheck passes. Independent scoped review and same-state screenshot recheck are pending; no visual PASS has been recorded.
Task 6: context recovery audit repeated after the user again flagged possible context loss. Coordinator re-read the source task, V3 acceptance contract, active delivery contract, global progress ledger, SDD ledger, fix brief, Git branch/HEAD/status, and desktop/mobile visual truth. The acceptance contract now contains one consolidated final-confirmation checklist and the hard invariant `0 <= error_count <= request_count`; current product scope remains unchanged and no prior confirmed decision was dropped.
Task 6: fix round 3/5 scoped re-review returned BLOCK with 0 Critical, 2 Important, 0 Minor. Production CSS satisfies both fixes, but tests do not protect desktop spacing or the full selected-underline/pointer-vs-keyboard focus contract. Fix round 4/5 must change only focused tests, then receive a fresh scoped re-review and same-state browser screenshot QA. Push, deployment, production access, and completion status remain forbidden.
Task 6: fix round 4/5 test-only implementation commits `d70a86d80`, `bd3fd09d4` strengthen desktop/mobile shell and Tab modality/underline gates; focused view spec reports 12/12 and typecheck passes. A mistakenly broad test command also exposed one pre-existing API-spec route expectation mismatch outside this round; the required focused gates are green. Independent scoped re-review is pending.
Task 6: fix round 4/5 (2 addressed, 0 open; commits `d70a86d80`, `bd3fd09d4`). Independent scoped re-review reports 0 Critical, 0 Important, 0 Minor.
Task 6: post-round-4 same-viewport combined comparison remains BLOCKED by one P2 mobile fidelity mismatch: the confirmed prototype constrains the description to `34ch`, uses 27px desktop / 23px mobile title sizing, and a 7px description offset; production uses a uniform 24px title, 4px offset, and unconstrained mobile description. The implementation therefore keeps the description on one line and shifts Tabs, filters, summary, and first card approximately 28–32px upward at 390x844. Fix round 5/5 is limited to these exact header typography/wrapping values plus focused tests.
Task 6: fix round 5/5 preparing a fresh screenshot-faithful implementer at base `bd3fd09d4`; this is the final allowed task fix round before adjudication. Push, deployment, production access, and completion status remain forbidden.
Task 6: fix round 5/5 implementation commits `db88d32e5`, `674ae5cb5` apply the exact prototype header typography/wrapping tokens and focused test gates; view spec reports 12/12, typecheck and diff check pass. Independent scoped re-review and post-fix same-state comparison are pending.
Task 6: fix round 5/5 (1 addressed, 0 open; commits `db88d32e5`, `674ae5cb5`). Independent scoped re-review reports 0 Critical, 0 Important, 0 Minor. Post-fix same-state browser comparison is the remaining Task 6 gate.
Task 6: round-5 same-state browser recapture did not close the visible P2: under the production font stack, `34ch` computes to approximately 300px and the 21-character Chinese description remains one line. Real DOM range measurement is 266px through `优先`, 280px through `级`, and 294px through `。`; the source wraps after `优先`. A same-round visual correction narrows only the below-760px description width to 272px, with focused tests, before the final screenshot recheck.
Task 6: round-5 visual correction uses `max-[760px]:max-w-[272px]`; focused view spec reports 12/12, typecheck and diff check pass. Final same-state browser recapture and independent correction review remain pending.
Task 6: final same-state recapture after pointer-focus correction now passes at exact 1440x1000 and 390x844; selected pointer tab has no focus rectangle, desktop cards remain 612px/two-column, mobile alignment matches the source, no horizontal overflow, and console has 0 warnings/errors. `design-qa.md` is now `passed`; final images are `implementation-v3-final-*` and `comparison-v3-final-*`.
Task 6: fresh final verification exposed a route-test fixture regression: `accountMonitorRouteRepoStub` relies on a nil embedded `AccountMonitorRepository`, so the new `ListTimelines` service call dispatches through a nil promoted method and panics. Production code is not implicated. Minimal test-stub repair is in progress; push, deployment, production access, and completion status remain forbidden.
Task 6: route-test fixture repair commit `0221b82c2` adds the explicit empty `ListTimelines` stub only; the previously failing `go test -count=1 ./internal/server/routes` now passes. Independent scoped review is pending.
Final fix wave: context recovery/root-cause audit completed after the user flagged drift risk. The V3 contract, active delivery contract, source task, plan, ledger, branch state, and final desktop/mobile evidence were re-read. Three independent review findings remain open: group tabs must sort by native `rate_multiplier` descending with native-order tie break; existing-account priority updates must reject values below 1 server-side; native upstream multiplier cost must remain eligible when same-window real usage cost is zero. Brief: `final-fix-brief.md`. Push, deployment, production access, and completion status remain forbidden.
Follow-up decision (2026-08-04): user added that upstream accounts without native multiplier data (commonly newapi) must support entering and editing a manual multiplier on the account card. The V3 contract now records manual override semantics (`rate_multiplier` + `rate_multiplier_policy=manual_override`), without changing procurement-cost mode or page scope. Brief: `manual-multiplier-brief.md`; implementation remains local-only and project status remains “进行中”.
Follow-up implementation: `1c739b174` adds manual source projection and finite/non-negative server validation; `7da5a826` records the RED/GREEN evidence. Card/View tests cover missing-native entry, edit, exact manual-override payload, local validation, and rejected-save draft/focus preservation. Fresh verification after the follow-up: backend four-package regression passed; frontend 33 tests passed; `pnpm typecheck` passed; `pnpm build` passed; diff check passed. Independent review/visual production evidence for this follow-up remains a local gate; no push, deployment, production access, or completion status change.
Final fix wave implementation and focused verification are complete locally: group tabs now sort by multiplier descending with native-order and ID tie breaks; single and bulk existing-account priority updates reject values below 1 before service writes; native multiplier cost remains eligible when same-window usage cost is zero while procurement zero-base cost remains ineligible. Handler/service/frontend focused tests, affected backend packages, frontend typecheck, and `git diff --check` pass. No push, deployment, production access, or online validation was performed; the overall delivery remains 进行中.
Deployment continuation (2026-08-04): user authorized deployment. Branch `codex/account-monitor-completion` was pushed at `e02914ac969c886cdeec5a2184dff422eb8d3ce0`; fresh backend four-package tests, frontend 33 tests, typecheck, build, and diff check passed. A current-tree 0600 evidence file was generated and the immutable candidate was preloaded. The production executor returned the fail-closed gate `migration_set_changed` with a 300-second unavailable estimate before candidate startup; production remained on `d4fb5e4a4` with PostgreSQL, Redis, Caddy, active API, and worker identities unchanged. User then explicitly authorized the bounded maintenance deployment. Maintenance release gate update is now in progress under `maintenance-release-brief.md`; overall status remains 进行中 until the new commit is pushed, deployed, and verified online.
Maintenance release gate (2026-08-04): TDD RED reproduced the retired `c618fc… -> e95b…` rejection in the host executor and the controller test failed while it still forwarded `c618fc…`; GREEN now limits the controller/executor to `e95b3512ccfc5b5103b4547857c437338921fd6bb463b7f2078c9ee24da4f0fc -> 337212b4af85839c9497d0fef3153e5c858bd976fed268086459c21a12abcc76`. Tests cover unauthorized/illegal targets, retired transition rejection, API/Worker-only maintenance, shared-service identity preservation, and API/Worker rollback. No push, production deployment, or online verification performed; status remains 进行中.
Production closure (2026-08-04): final branch commit `82095b80770236eac24adb0bdb1b80cd639675cb` was pushed and deployed. Production `release-state` records `source_commit=82095b80770236eac24adb0bdb1b80cd639675cb`, `source_tree=3678c0b8c0427996327fe77ae126640a7e216868`, `migrations_hash=337212b4af85839c9497d0fef3153e5c858bd976fed268086459c21a12abcc76`, `active_slot=green`, and image/image ID `release-82095…-12585… / sha256:12585…`. PostgreSQL `2db52788…`, Redis `c45202c0…`, and Caddy `ace4a23b…` match release-state and were not rebuilt. The production page returned 200; 24h/7d/30d monitor APIs each returned the matching range, 7 groups, and 66 accounts. All seven group summaries are present; cards expose priority, score, rank, procurement cost, and multiplier; each of 7 groups has `rank_order_violations=[]`; concurrency for accounts 20/118/21 is `current=0`, `limit=10`; and `procurement_cost_cny`, `procurement_cost_effective_at`, plus the non-negative procurement-cost constraint are present. Account Monitor V3 is complete. Independent revenue, accounting, reconciliation, and Feishu work remains open under its own ledger items. See `production-verification.md`.
## 2026-08-04 状态与单卡刷新生产缺陷修复

- 状态：进行中。
- 历史生产事实：提交 `82095b80770236eac24adb0bdb1b80cd639675cb` 已推送、部署到绿色槽位，并完成页面、24h/7d/30d、7 个分组、66 个账号、迁移和并发端点验证。
- 新增缺陷：账号 #118/#119 在 24 小时真实请求分别为 78/742 且失败均为 0 时，latest probe 失败仍会把窗口服务状态投影为 unavailable、取消排名；单卡刷新后 `latest.checked_at` 已更新而卡片 `checked_at` 仍使用旧真实请求时间，同时前端运行超时与关键重载结果缺少明确反馈。
- 修复边界：真实请求窗口状态优先、探测事实保留、`checked_at` 与 `evidence.observed_at` 分离、单卡专用超时及 POST 后重载成功/失败反馈；不改评分、成本、并发、分组排序、schema 或营收/账务边界。
- 完成门禁：实现、独立复审、完整验证、推送、部署并针对 #118/#119 完成线上复验之前，总账保持“进行中”。
- TDD RED（2026-08-04）：后端新增 `TestAccountMonitorWindowStateUsesRealRequestsAndKeepsLatestProbeTimeSeparate` 首次运行失败，#118 仍为 `service_state=unavailable`/未排名；前端 API 单测仍观察到旧 runOne POST 未带超时，AccountMonitorView 两个刷新用例分别缺少明确失败/成功反馈。
- TDD GREEN（2026-08-04）：后端状态投影、前端专用 240 秒单卡超时、POST 后重载结果处理及合同/总账更新完成。聚焦后端回归通过；完整后端 `internal/service`、`internal/repository`、`internal/handler/admin`、`internal/server/routes` 通过；前端 34 个聚焦测试、`pnpm typecheck`、`pnpm build` 和 `git diff --check` 通过。构建保留既有 Browserslist/动态导入/大 chunk 警告，不影响退出码。
- 当前提交尚未推送、部署或生产复验；待协调任务独立审查后继续执行发布门禁。
- Reviewer fix round 1（2026-08-04）：移除 `accountMonitorWindowSuccessSamples` 的倍率推算，`real_requests` 证据现在严格使用原始 `SuccessCount`；`evidence.observed_at` 只接受真实请求 `LastObservedAt`，缺失时保持零值。新增不一致聚合回归（`RequestCount=3`、`SuccessCount=0`、`SuccessRate=0.5` 必须 unavailable/unranked）并补齐历史服务/handler 测试夹具的原始成功数。聚焦 backend service/handler/routes、frontend 34 项测试及 diff check 通过；仍待协调任务独立复审、推送和部署。
- Production verification（2026-08-04）：提交 `aca3f915e88ae4e6311ca3a089ae638129658b4e` 已推送并部署到 blue，PostgreSQL/Redis/Caddy 身份未变化；#118/#119 的真实请求状态与单卡检查时间推进已在线复现通过。但最终整体验收仍 BLOCK：真实请求达到 3 次时不应被 probe stale/latest 缺失压回 pending；真实请求仅 1–2 次且 probe 聚合/最新记录发生竞态时不得绕过 latest-probe 门槛。Final fix wave 1/1 从 `aca3f915e` 开始，必须 TDD 修复、独立 scoped re-review、重新推送部署和线上复验；总账继续保持“进行中”。
- Final fix wave 1/1：实现代理准备从 `aca3f915e` 开始，需求文件为 `status-refresh-final-fix-brief.md`；只处理最终审查的两个 real-request threshold Important，不扩展范围。
- Final fix wave 1/1：实现提交 `3880d2e599b2d0648982df17922d43ce8b60889d`。TDD 覆盖“达到 3 次真实请求时忽略 probe stale/latest 缺失”和“仅 1–2 次真实请求时继续服从 later latest-probe 门槛”；完整 `internal/service` 测试通过。Scoped re-review 对两个 Important 均判定 ADDRESSED，且无新增 Critical/Important、无越界观察。状态仍为“进行中”，待最终提交全量验证、推送、重新部署与线上复验。
