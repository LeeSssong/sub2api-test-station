# SDD ledger — plan: docs/superpowers/plans/2026-07-31-account-rate-multiplier-sync-implementation-plan.md

Setup: isolated worktree `codex/account-rate-multiplier-sync` verified.
Setup: design, implementation plan, and project progress registration prepared.
Task 1: implementation committed as c8ad0096c; independent review dispatched.
Task 1: initial review clean; follow-up diff 78a06a011 (malformed present field classification) requires scoped independent review before completion.
Task 1: complete (commits 903bcf5..3b6a669, initial and follow-up reviews clean).
Task 2: base 3b6a669; implementer dispatched.
Task 2: fix round 1/5 started from bc527ec (3 Important findings open: decimal(10,4) quantization/idempotency; scheduler cache UpdatedAt fencing; real PostgreSQL/Redis integration coverage).
Task 2: fix round 1/5 implemented. Precision normalization, Redis UpdatedAt/version fencing, tombstone-safe delete/invalid-payload handling, and PostgreSQL/Redis integration tests added. Non-integration regressions GREEN; integration execution blocked by exact harness panic `rootless Docker not found`.
Task 2: complete (commits 30d29a8..fd739a8; all 3 Important findings addressed in scoped re-review; real integration tests remain environment-blocked and must run before deployment).
Task 3: lifecycle/periodic probe integration implemented locally. Eligible OpenAI API-key account create, edit, and probe-enable actions asynchronously force one lifecycle-native billing probe; inactive accounts are rejected at the lifecycle service boundary. Scheduled probes retain the existing snapshot persistence/synchronization route, and no ordinary forwarding path was modified. Targeted regressions are GREEN; independent review, push, deployment, and live verification remain pending.
Task 3: fix round 1 addresses the independent-review TOCTOU finding. Lifecycle mode now rechecks active status from the account reload immediately before HTTP; a deterministic active-first/inactive-second test is GREEN with zero upstream requests. Manual forced-probe semantics remain unchanged. A scoped re-review, push, deployment, and live verification remain pending.
Task 3: complete (commits c3f1aaa..11bb828; TOCTOU finding addressed and scoped re-review approved; push, deployment, and live verification remain pending).
Final review: rejected at bee5fe2e3 with 3 Important findings: ordinary create/edit silently infers manual override; Redis UpdatedAt fencing is not commit-order monotonic because PostgreSQL NOW() is transaction-start time; account-ID-only singleflight merges manual/scheduled/lifecycle semantics.
Final fix round 1/1: started from bee5fe2e3; one fresh implementer will address all 3 findings, followed by one scoped independent re-review.
Final fix round 1/1: local implementation complete. Explicit caller-controlled multiplier policy now defaults new accounts to managed, preserves omitted-policy edits, rejects invalid/manual-without-value intent, blocks `extra` smuggling, and round-trips through shared create/import/duplicate/bulk paths plus the standard OpenAI API-key UI. Account-row SQL mutations now advance durable `updated_at` with `GREATEST(clock_timestamp(), updated_at + interval '1 microsecond')`; normal edits derive and persist the next version while holding the row lock, and an integration regression covers an older probe transaction committing after a normal edit. Probe singleflight keys now include manual/scheduled/lifecycle mode and audit triggers match the executing mode. Required backend tests, focused frontend tests (51), Vue typecheck, repository tests, and integration-tag compilation are GREEN. Real PostgreSQL/Redis integration execution remains blocked by the exact harness panic `rootless Docker not found`. Status remains 进行中 pending scoped independent re-review, push, deployment, and production verification.
Final fix round 1 scoped re-review: Finding 3 ADDRESSED; Findings 1 and 2 NOT ADDRESSED due omitted-policy stale overwrite under row-lock concurrency and remaining Ent account mutation paths using non-monotonic `time.Now()` updates. Minor stale lifecycle comment noted.
Final fix round 2: started from 1f6c5f286; scope narrowed to locked policy preservation, remaining Ent account updated_at writers, and lifecycle comment cleanup. Fresh implementer and scoped re-review required.
Final fix round 2: local implementation complete. Locked-row policy is preserved for omitted intent and explicit intent is transient with `json:"-"`; remaining Ent account mutation publishers use the shared database monotonic update helper; focused and required backend tests pass; integration-tag compilation passes; real container execution remains blocked by `rootless Docker not found`. Awaiting scoped re-review.
Final fix round 2 controller validation: with Colima `DOCKER_HOST`, Ryuk disabled, local PostgreSQL 18.4 and Redis 8.8 test images, all three focused real PostgreSQL/Redis integration regressions passed (`ok .../internal/repository 4.778s`). Temporary Redis test tag was removed. Awaiting scoped independent re-review.
Final fix round 2 scoped re-review: two findings ADDRESSED; one Important remains NOT ADDRESSED — `UpdateLastUsed` still uses Ent `UpdateDefault(time.Now)` before publishing the account-last-used outbox event, so durable account `updated_at` monotonicity and cache fencing are not guaranteed.
Final review gate: BLOCKED / do not merge. The single scoped re-review found a load-bearing residual Important; this branch remains `进行中` and requires a separately approved implementation/review cycle before integration, push, deployment, or production validation.
Final fix round 3: `UpdateLastUsed` now uses the shared monotonic account-row SQL update before publishing its existing last-used outbox event; a sqlmock regression covers both the durable version expression and payload. Focused repository tests pass; awaiting independent review.
Final fix round 3 independent review: APPROVED. The requested `341a6c902..8d6a1f8d8` range and the range-start fix commit `341a6c902` were reviewed together; no Critical or Important finding remains. Focused and full repository tests pass, and the three focused real PostgreSQL/Redis integration regressions pass through Colima (`ok .../internal/repository 3.433s`). Branch is merge-ready after the review commit; project status remains `进行中` pending server push, deployment, and production verification.
