# 星桥原型一致性修复验证

日期：2026-09-20（America/Los_Angeles）

范围：用户本轮截图所指的侧栏、管理员上传Logo、AI工具卡片、线路详情、线路检查与关联密钥流程。依据星桥UI项目的原型结构文档、prototype交互原型及DESIGN.md。没有改动这些基准。

## 候选状态

- 工作区：`/Users/awen/Documents/ChatGPT/sub2api-prototype-parity`
- 分支：`codex/prototype-parity`
- 基线：`9bc887c4e628313f2f1b5363582c0e8481c240b1`
- 修改未提交、未合并、未推送、未部署；根main未修改。依赖软链node_modules、生成dist不是推广制品。

## 已实现

- 侧栏242/200/76px、24px品牌Logo、版本号、雾青选中态、整行余额与充值兑换、独立头像用户名客服。
- 用户Logo及favicon优先公共site_logo配置；真实MIME识别，清空配置回退默认PNG，不使用写死用户SVG。
- 原型四卡信息层级与关联密钥按钮；关联数包含禁用密钥；不再直接跳转密钥页。
- 深色线路详情、1h/24h/7d三段控件、真实P50与请求次数、质量排序、每行预选关联、即时结果独立显示。
- 单列紧凑创建表单，原生keysAPI，限制项、搜索、失败保留输入；从详情取消后返回原行，键盘焦点保留。
- 我的AI线路只显示已关联线路；初始本次检查为—；检查调用后端原生探测，不使用用户key。未知、失败、超时和停用分开表达。
- 后端检查限定用户可访问组和组内可调度账号/模型，2worker、全局4批、30秒冷却、45秒批截止与15秒首字截止。手动观测限定group_id，避免共享账号跨组污染。
- 管理员组编辑新增显式工具关联，版本化与审计；没有按服务商或名称自动推断。
- 首次请求失败不显示假零计数；刷新失败保留旧数据；后台观测5分钟有效性与15秒首字阈值。

## 验证

- 相关前端9个文件57项测试通过；后续焦点/新鲜度修正的Dashboard与BaseDialog测试8项复跑通过。
- vue-tsc、定向ESLint、i18n完整性3项、Vite生产构建通过。pnpm包装器尝试重装软链依赖被阻止，实际按package.json同等步骤直接执行本地vitest/vue-tsc/vite，未重装原工作区依赖。
- Go service/repository/handler相关MonitorV4、AccountMonitor、manual check、mapping测试通过；manual/check race通过。
- 独立postgres:16-alpine、无网络/无发布端口/无宿主数据挂载：17项真实SQL断言通过，含迁移238/239/240、三个窗口P50、跨组隔离、时效/超时、映射审计和快照读写；测试容器及卷已删除。见backend/scripts/prototype-monitor-postgres-results.json及可复跑脚本。
- 浏览器1730×1000：侧栏242px、四卡、详情七列、关联创建与取消返回、焦点回原线路按钮、检查成功/超时分别显示；390×844侧栏76px，无页面横向溢出；1395×675展开所有高级项后仍能滚动到有效期及底部操作。
- 附件截图使用固定测试数据/拦截API，只用于界面与交互验证，绝非测试站真实数据或线上验收。
- Impeccable指定改动目标检测结果为空数组；git diff --check通过。

## 既有失败与未验证项

- 旧HybridPerformanceView测试4项在原始main和候选分支均失败（旧测试未解析HybridPerformancePanel），未混称本次通过。
- 未执行测试站/主站发布、站点数据库迁移或真实付费上游探测；未验证Safari收藏/固定标签图标缓存更新。代码层favicon配置/MIME行为已测试。
- 旧版本没有保存显式工具映射。发布前/后需管理员在组编辑的“AI工具关联”明确配置；未配置工具卡片不推断线路。
- 原生已删除组会解绑密钥并丢失身份，现有历史无法从UI恢复；本次保留停用且关联仍存在的组，但未实现未来删除身份快照合同，因此不能据此声称整个结构文档全部验收通过。
- 即时检查结果立即返回；成功率/P50随原生下一轮统计快照更新。

## 发布交接

依据测试服AGENTS.md，仅发布总控能整合根main和执行发布；需明确提交/推广/部署授权，并遵守根main干净、HEAD/tree与已推送origin/main相同的门禁。三个SQL迁移为新增字段/表，238额外清除旧可用性flag，等待新规则下一轮刷新。不可推广fixture、依赖软链、浏览器测试认证占位值或临时容器数据。
